# AI Session Log — 2026-04-06

## User request summary
- Audit the system against the coursework PDF and identify remaining gaps
- Add the parts directly reusable in the poster and portfolio
- Export reusable screenshots from a fixed demo route
- Assess whether tangible (NFC) deployment is feasible in time

## Prompt used (paraphrased, not verbatim)
> "Add a `?demo=video` query parameter to `src/App.jsx` that seeds a fixed set of unlocked keepsakes and a short reply thread, so we can capture screenshots that always show a populated state. The demo flag must not pollute LocalStorage in normal sessions."

## What the AI returned
- A small effect block that reads `URLSearchParams`, seeds in-memory state when `demo=video` is present, and skips the LocalStorage write
- Three pre-baked example messages and two unlocked keepsakes for the screenshot-friendly state

## Changes we accepted vs. rejected
- **Accepted:** the query-param gate and the in-memory-only seeding
- **Edited:** scoped the demo seed to a single `useEffect` so it cannot fire twice; added a visible "Demo Mode" pill in the header so reviewers can see the state is synthetic

## Changes made in this session
- Added demo-state query support in `src/App.jsx`
- Added seeded demo messages for screenshot-friendly states
- Generated poster / portfolio screenshot assets
- Added `public/tangible/tag-sheet.svg`
- Added `DEMO_GUIDE.md`
- Rebuilt the portfolio draft and supporting graphics outside the system repo

## Verification performed
- Ran `npm run build` successfully (no warnings or new errors)
- Captured real screenshots from the fixed demo routes on Pixel 6 and desktop Chrome
- Confirmed that visiting the site without `?demo=video` does not write any seeded data

## Notes
- Earlier sessions (3D scaffold, NFC hook, portfolio CSS) are now backfilled in `session-2026-03-25.md`, `session-2026-03-28.md`, and `session-2026-04-02.md`
- Demo Mode is for screenshots and presentations only &mdash; do not ship the seed data with a public release

